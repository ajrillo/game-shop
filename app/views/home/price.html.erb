<h1 align = "center">Browse Avalible Prices</h1>
<center>
    <table cellspacing="20">
      <thead>
        <tr>
          <th>Game Name</th>
          <th>Game Price</th>
        </tr>
      </thead>
    
      <tbody>
        <% @prices.each do |price| %>
          <tr>
            <% for game in @games.each do %>
              <% if price.name == game.name %>
            <td> <%= game.name %> </td>
            <%end%>
            <%end%>
            <td>  $<%= '%.2f' % price.price %>  </td>
          </tr>
        <% end %>
      </tbody>
    </table>
</center>

<br>
<%= link_to 'Back', root_path %>